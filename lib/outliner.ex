defmodule Outliner do
  @moduledoc """
  Algorithms which re-format an outline into its inherent organization.
  """

  # Convert a list of paragraphs into the standard list of sections.
  def or_oar(paragraphs) when is_list(paragraphs) do
  end
end
